# Stage 20.0 — A1X independent experiment line

This checkout is independent of hominal.cc1 (Stage 10.3). Never edit, deploy, reset, or operate the other line from these tools. Source provenance is in lineage/stage20-fork.json. New code, private configuration, application profiles, runtime state, budgets, and archives must have their own Stage 20 namespace. Share research conclusions only; importing a change requires explicit review, never live source synchronization or personal-memory copying.

Use one cognitive owner and the existing factual/learning/recovery contracts. Peripheral vision supplies evidence and hypotheses; it does not own goals, self, or independent actions. Distinguish completed input delivery, observed effects, and semantic success. Unknown external effects must not be replayed automatically.

Preserve Bazzite, KDE, game mode, and working GPU/input/network drivers. Use the ordinary desktop identity for desktop work and keep application/model dependencies in user space. Do not disable SELinux or browser sandboxing. Secrets, screenshots, private accounts, model files, and life archives do not enter Git.

User-authorized runtime roles are `fast`, `main`, and `high`; startup configuration maps them to exact public llmserver model IDs and selects one supported reasoning effort for each role. The model catalog keeps one entry per public model ID and lists every supported reasoning effort. The current mapping is fast → codex-luna/none, main → codex-terra/none, and high → codex-sol/low. Rolling hour USD 5 and rolling 24-hour USD 50. Continuous Stage 20 experiments share the line's rolling ledger; a new sample must not bypass this ceiling.

Report code, engineering validation, deployment, and life evidence separately. Do not claim Stage 20 success from screenshots, correct JSON, more actions, or growing memory counts. Read plans/stage-20.0-development.md and plans/stage-20.0-experiments.md before running experiments.
