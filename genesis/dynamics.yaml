schema: hominal.genesis.dynamics/v1
dynamics_id: g0-dynamics-v001
time_unit: minute

pulse:
  interval_seconds: 5

affect:
  return_rate: 0.10

concern:
  base_drive: 0.15
  urgency_weight: 0.85
  growth_gain: 0.25
  resolution_gain: 0.40
  natural_decay_rate: 0.02

attention:
  affect_weight: 0.30
  value_weight: 0.22
  novelty_weight: 0.15
  resource_cost_weight: 0.25
  trigger_threshold: 0.45
  revisit_seconds: 10
  maximum_idle_seconds: 10

difference:
  accumulation_decay_rate: 0.35
  learning_rate: 0.12

exploration:
  unknown_growth: 0.10
  relief: 0.45

value_field:
  idle_growth: 0.18
  activation_gain: 0.30
  activation_return_rate: 0.08
  satiation_gain: 0.25
  satiation_return_rate: 0.15
  orientation_gain: 0.03

integrity:
  persistence: 0.85
  gap_gain: 0.50
  repair_gain: 0.40
  mirror_threshold: 0.60

bounds:
  signed_value: [-1.0, 1.0]
  unit_value: [0.0, 1.0]
