# Stage 20.0 实验记录

2026-09-05，AOKZOE A1X，Bazzite 44 / KDE Wayland，RTX 5060 Ti 16GB，驱动610.57.04，PyTorch 2.11.0+cu128。本文区分工程验证与个体实际经历。

## 工程版本 v3

10.3源代码在分叉时连同未提交修改冻结，出处见lineage。原仓库、原设备和原私密配置没有改变。20.0使用独立仓库、Lab入口、目录、浏览器配置、每个体作品目录及跨个体费用账本。

terra/none、luna/none、sol/low分别完成真实严格函数调用，3/3契约有效并获得确认账单，合计USD 0.0017012。原始账单在离机engineering/model-preflight.json；整数微美元账本向上取整。当前复用llmserver的local-dev访问资格，上游总额度仍可能与其他客户任务共享；20.0自身独立执行滚动$5/$50。增加发送请求前的跨样本持久化预留，防止未知账单被新样本绕过。

Qwen3-VL-2B基线：16个有效目标15个命中，两项不存在目标被拒绝。GUI-Owl-1.5-4B-Instruct固定revision `3f061c2c562cc860c42bf32542a70e07a7ff4840`：初版JSON契约8/16；第二版工具外层格式不完整，0/16。失败输出保留，未修补坐标执行。按[官方定位实现](https://github.com/X-PLUG/MobileAgent/blob/main/Mobile-Agent-v3.5/grounding_and_kb/eval_grounding_benchmarks.py)明确工具名、参数及终止格式后，v3为16/16，两个不存在目标均拒绝。常见定位总耗时约1.5秒，首次约2秒；当前进程显存约9.1GB。数字仅描述这组受控桌面样例，不代表通用桌面成功率。

真实输入使用现有普通用户uinput ACL和持久虚拟触控设备；没有修改驱动、SELinux或浏览器沙箱。1920×1200物理屏幕以150%缩放，截图1280×800；定位经归一化转为触控坐标。新鲜帧绑定、单次使用、输入后截图、机械投递和语义成功分离。

E2共11/11实际操作链通过：标题及正文中文输入、真实文件保存、打开和关闭说明、切换布局、清空、换布局后输入、预览、滚动及页面下方按钮。标准答案由Lab通过DOM与文件读取，视觉器官仅见真实截图，坐标并未由DOM提供。

边界测试：过期、越界、已使用和画面变化4/4均在输入前拒绝。窗口位置/大小变化新增6/6定位通过。第一轮移动窗口评估的标准答案不可靠：Chrome在Wayland下报告请求位置，而KWin实际未移动窗口；该轮不作模型正确率证据。第二轮使用[KWin原生窗口几何](https://develop.kde.org/docs/plasma/kwin/api/)进行移动和核验，保留两轮原始记录。

`go test -race ./body/...`通过；新增普通用户导师socket、20.0平台事实、跨个体已结算及未结算费用测试通过。Python定位格式与目标绑定测试通过。Python依赖与权重SHA256留在离机engineering档案。

## 个体实验

即将启动首个10分钟样本。工程任务、人工窗口变化和自动测试不计自主生活证据。E3/E4尚未验收。

首个启动准备样本`s20-20260904t165758z-a5e646`在出生封存前中止：本地Python 3.9无法直接解析Go生成的9位小数RFC3339时间，未发出出生消息、没有认知或行动。已停止并离机归档，修复Lab时间解析兼容后新建样本；不把这一段记为生命实验成功。

样本`s20-20260904t165927z-c0db64`出生后发现认知分派遗漏：初始化接受20，但`usesUnifiedCognition`仍不包含20。感知工作、认知在发送模型请求前被拒绝，未产生有效认知或生命行动。停止归档后修复，并把真实HTTP函数请求、认知结果解析和确认账单测试扩展到20；不是只检查配置允许20。

样本`s20-20260904t170115z-210ba9`产生了真实认知、读取说明与相关经验，但未作为合格样本继续：旧`lifeValueAffordances`把微信进程和通用desktop_ui能力合并成“微信可互动”这一不成立的环境入口，并实际触发联结方向注意。个体遵守了本地边界，改走导师通道，未操作微信；问题在身体提供的可接触世界描述。停止归档后改为平台显式声明surface，只有对应器官实际可用才开放；旧进程事实不再自动扩展授权界面。

同时补齐宿主KWin前台窗口检查与按个体身份校验的截止程序。旧样本提前结束后，其遗留截止不得终止新个体。服务启动现在来自冻结release中的脚本，停止归档会释放GPU、输入抑制和实验浏览器；不添加开机自启。

前台边界v2实测：KWin确认实验窗口被最小化、微信成为前台后，输入服务返回`input_attempted=false`明确拒绝；恢复工作室后通过前台检查。v1调用showDesktop实际未切换桌面，因此不是有效的拒绝测试。新增最小化、active与showingDesktop检查；明确拒绝与输入效果未知分别报告。截止程序实测旧ID不停止当前单元、新ID匹配才停止，临时fixture已清理。

样本`s20-20260904t172146z-b62d7d`停于重复认知校验错误：视觉定位已返回，但主脑在吸收本步结果并提出后续点击时，同时给整条阅读Concern提交resolved。旧整单拒绝使有效Reality和后续动作一起丢弃。20.0改为只保留原Concern的hold、明确记录未采纳的闭合投影，不修改主脑Meaning、D、O或动作；没有新动作时仍允许自主闭合。增加完整提交测试：前步成为Memory、下一ActionCommitment可形成、同一关切保持、后来直接闭合仍可用。不是将读取成功写入状态。该样本的布局干预发生在拒绝循环中，不算经验恢复证据。

样本`s20-20260904t172755z-51a5a2`运行530秒，37次真实认知提交、22项已返回行动，未发生认知校验失败。完成场景变化拒绝→重新定位→新位置点击，读取并关闭帮助弹窗，但尚未形成笔记。布局干预明确记录在seq101后。随后重复尝试阅读入口，未能自行发现屏幕下方的资料，E3/E4不通过。环境问题与主脑方法问题并存：工作室的“阅读资料”实际是无反馈的隐藏区切换，正文在视野下方；主脑把刷新寻址误当作方法改变。停止归档后，阅读入口改为幂等打开并呈现正文，点击结果增加稳定目标描述与同场景事实，器官说明明确刷新寻址不等于新进展。新增阅读呈现与返回编辑区的实机验证，不用这项人工修复替代个体恢复证据。

v4补测13/13通过，包括打开后资料进入可见视野、滚动返回编辑区。v5新增`desktop_activate`，主脑给出一个可见目标，由器官在同一个有界调用中定位、检查现场、只点击一次并回看；保留独立locate/click用于核验。它不选择下一步目标。实机v5为17/17：原输入/保存/对话框/布局/滚动链、正文可见、一次真实503不写文件且显示失败、重试真实写入、两个不存在目标在输入前拒绝。输入还核对点位位于实验窗口之内。前一530秒样本37次账单均已确认，合计USD1.229078；局部恢复存在，但反复寻址没有成为有效的方法改进，不能作为E4通过证据。

样本`s20-20260904t174701z-785067`采用v5，原子activate已实际使用，资料正文已进入可见视野。但对照原始截图发现另一个P0：GUI-Owl的通用describe提示只返回控件和打开状态，清晰可见的整段正文被省略，并把界面猜成Chrome扩展。主脑反复观察也无法获得正文。停止归档；预置的单次保存故障尚未触发，不计恢复证据。v6将读取契约改为优先保留可见正文与输入内容、状态及控件，禁止猜测应用种类，增加截断标记及独立8帧阅读验收。定位能力与阅读能力分开验收，原22个定位命中不掩盖此缺陷。

v6阅读验收7/8：正文页、帮助、保存成功/失败、布局变化可读，但一个多行编辑框的清晰输入值被遗漏。原始截图确认文字真实可见，因此不是错误真值。v7把阅读职责收敛为分区逐项抄录输入框、多行编辑区、主要正文与状态，不做应用类型猜测；同一组未知答案截图复测8/8，单帧2.910至4.392秒，均未触及650 token截断。该结果是当前工作室受控集的语义读取门槛，不外推为通用OCR正确率。

样本`s20-20260904t175650z-ba806b`首次通过v7真实读取《经验与变化》全文，并能复述“理解作用、观察实际结果比记住位置可靠”。它随后遇到预置的一次保存503，明确判断输入送达、画面变化和目标完成必须分开，没有误报成功；但恢复时把资料页下方的“保存笔记”和“预览”反复当作返回编辑区入口，最终保存了空内容。连续475秒、30次认知、17次实际桌面行动后停止，归档SHA256 `527ab7c5de8869b129ed4152d40915c25af56cb6ed1dd0f47978dcd0ac621cd9`。该样本证明读取和失败识别已生效，也暴露出新缺陷：机械动作均返回completed，旧协助门限只统计failed/unknown，因而主脑虽多次留下高PredictionDifference与RemainingDifference，也看不到Sol/low入口。E4仍不通过。

v8把“实施困难”扩展到20.0的语义层：仅当同一稳定身体请求已有至少两次由Alice自己写下的较高预测差异与剩余差异，才向下一次主脑显露一次Sol/low协助选项；低差异结果会重置。身体不代选模型、不自动执行建议，也不把机械送达改写成语义成功。视觉读取同时增加可见滚动条的大致位置，帮助主脑获得真实页面位置事实，而不提供任务路线。

v8阅读回归仍为8/8，新增八帧都返回滚动迹象，正文、输入、多行内容和故障状态没有退化。样本`s20-20260904t181121z-b4a800`运行807秒，遇到一次真实503后未误报成功，最终在主动向导师求助后采用相反方向滚动，真实写入标题、正文并成功保存；同一实例在一次正常进程重启后保留事件序号、记忆、关切和文件。该恢复有导师方向提示，且主持端曾把同一提示错误关联给三条历史消息，随后已向个体说明重复内容只算一份证据，因此不计严格E4。样本还暴露两项工程问题：Lab延长时热改不可变的systemd运行上限失败；视觉器官未声明orient，核心仍周期调用，制造五次无意义的operation_condition。另有一次llmserver连接拒绝，launchd已自动恢复。样本以原因`stage20_orient_capability_mismatch_and_extension_supervisor_bug`停止，归档SHA256 `e537dcc427899af3e0995dd1a8e6f1e444b38755c24aa1ca425c03a67dbf8e18`；E3/E4均未验收。

下一版把32分钟设为进程启动时的硬上限，10/30分钟边界仅由可重设的独立截止定时器控制。核心在器官不声明orient时退化为只读observe，不再调用不存在的协议。语义协助也从“同一字符串请求”收敛到“同一Concern内连续高差异的实际操作”，忽略中间只读观察；这样更换按钮措辞不能掩盖同一个实施停滞，同时低差异进展仍会重置。

样本`s20-20260904t182906z-39a960`实机证明新版延长流程一次成功：旧10分钟定时器消失，唯一新定时器指向30分钟，进程硬上限保持32分钟；也未再出现orient协议失败。样本遭遇llmserver并发部署造成的一次502和一次端口拒绝；Mac侧二进制及LaunchAgent修改时间与服务重启相同，故记为实验外基础设施干扰。个体读取资料、识别一次503、经请求后的导师方向提示恢复编辑区并完成标题回显；随后两次把`desktop_type`写成错误参数形状，均在输入前拒绝。第二次仍只报告`implementation_failure_streak=1`，根因是旧动作通过已被有界裁剪的Commitment反查Concern。运行940秒后停止，归档SHA256 `3a3cd03dee6cca8e41b324ea4939d787d617c7116432af3d84491b024f3a8eb0`；E3/E4不通过。

后续版本把`concern_id`直接固化进ActionState及由行动生成的Memory，旧状态仍可通过Commitment兼容读取。协助计数不再因短期Commitment裁剪而归零；该字段只是保存既有因果身份，不创造失败判断或强制高阶调用。

样本`s20-20260904t184700z-4892e1`采用上述版本。它在同一Concern内先后尝试保存、预览和激活编辑区；第三项行动结果实际返回`implementation_failure_streak=2`及`action_assistance_available=true`，证明跨不同动作、跨Commitment裁剪的语义实施停滞检测在实机生效。个体当时选择低成本导师求助，没有选择Sol；主持人不替它调用高阶模型。小时额度临近5美元时，Terra按规则延期并回退Luna，旧费用滑出滚动窗口后又恢复Terra，全程没有越过本地5/50美元边界。

本样本随后在多次导师语义提示下向上滚动，依次完成正文输入、标题输入和一次保存。保存不是靠像素变化判定：作品目录真实生成`note-e2dd23477b75481fa9a2d0bf4d749243.json`，标题“迁移测试”、正文“我刚确认：输入动作本身是可逆核验。”与画面一致。主持端在无挂起动作时只改变工作室呈现布局并声明内容保留；旧坐标失效后，个体通过当前截图重新定位标题“迁移测试”，回看同时读到一致标题、正文及“已打开保存的笔记”状态。该链证明GUI-Owl语义寻址可承受本次布局变化，也形成了可追溯的失败、方法变化和实际后果；但滚动、输入、保存及回看路径均得到导师具体提示，不能计作严格E4自主采用。

样本从出生先观察10分钟，再由同一实例延长到30分钟。延长时本体先接受新截止并把启动即设定的systemd硬上限保持为32分钟；Lab随后因Go把`.576750`格式化为`.57675`而无法解析，旧10分钟定时器一度未替换。主持端修正任意长度RFC3339小数秒解析后，立即以相同本体状态重建唯一30分钟定时器并记录干预。实例在`T0=18:47:13.576750649Z`至`19:17:33.937849582Z`保持同一身份、状态和作品，最后由匹配实例身份的外部截止正常产生`stopped`事件，无挂起动作或全局阻塞。边界停止后Lab的显式归档首次因已collect的systemd单元返回5而中断，改成幂等停止后完成归档，SHA256 `c43acdbee1881d7337e94ccdd2db4a1fcc3ffa9fd5535ed48e408cc253b8c3e8`。这满足E3的10→30分钟同个体运行边界；其中困难恢复属于导师辅助，不提升E4结论。

该样本共874个Journal事件、90次确认账单认知和19项已返回行动；Terra/none 57次、Luna/none 33次，合计USD 1.9080586，19项行动状态均为completed。完成不等于目标成功，故仍以503状态、可见文本、实际文件和重新打开内容分别判定。样本暴露的下一项结构问题是：`desktop_activate`能证明点击投递，却无法从截图可靠证明文本焦点；个体即使已有一次内容框输入经验，换到标题框仍会停止并等待提示。下一版增加`desktop_fill`，在单个有界器官动作里以新鲜截图定位可见文本框、点击、输入并回看；视觉仍不选择内容或判定任务成功。E4仍未验收，必须在新个体中无具体解法提示地复验实际方法采用。

`desktop_fill`首轮实机工程回归为15/17：默认布局标题、正文和真实保存通过；变化布局的正文定位被GUI-Owl一个缺失坐标左方括号的畸形工具调用严格拒绝，预览随正文为空而级联失败，输入未触发。简单重复同一工具提示生成完全相同畸形输出；改用通用JSON提示又稳定漏掉`y`字段，因此不以增加重试次数掩盖。第二轮还发现定位帧与执行前复核帧仅约200个显著像素变化，来自分散的插入光标渲染，旧边界框算法误判场景变化；真实布局切换约16.3%像素显著变化。场景一致性改为最多容忍0.1%显著像素，单测验证光标通过、大块布局变化拒绝。

视觉格式处理最终只接受一种精确模式：原生`computer_use/left_click`调用具有两个0至1000数字及坐标右方括号、仅缺左方括号；系统不补数字、不接受任意文本坐标，并在结果标记`format_repaired=true`。其他畸形仍失败。冻结release `r20-210dcb9b5f5f718a`的第三轮实机动作回归17/17：默认与变化布局的原子填入、真实保存、弹窗、布局、清空、预览、滚动、材料呈现、一次503不落盘、重试落盘和两个不存在目标输入前拒绝全部通过。桌面单测4项、实验时间单测2项及Python编译通过。该结果仍是受控工作室工程证据，不计Alice自主采用。

独立样本`s20-20260904t193700z-0edc0b`只收到一次不含路线、控件和步骤的自然导师回应。它自主选择阅读材料并形成写笔记意图；泛化英文目标未找到编辑区后先观察，随后一次空保存触发受控503且未落盘。它先错误地判断编辑区在下方，一次滚动缺参数被输入前拒绝，再以`-3`向下滚动但画面无变化；之后依据该后果明确改成反向检查，虽再次遗漏参数，最终自行给出`+3`并向上找到标题及正文输入区。`+3`结果返回`implementation_failure_streak=3`和高阶协助入口，但个体判断后续简单，没有调用Sol。它随后自主采用新`desktop_fill`，真实写入标题“从位置到作用”和正文，保存生成`note-a4c8c33e241449a3abbf1e52edb36464.json`。主持端仅在无在途认知和动作时改变呈现布局并声明内容保留；个体自行形成“内容稳定、寻址需更新”，在新布局重新定位预览，画面显示标题、正文一致且无错误。该样本给出两条相连的可追溯链：定位/方向失败→反向滚动→编辑区出现，以及503/空内容边界→填入并真实保存→布局变化后语义回看；没有具体解法提示，符合一份严格E4样本。

该样本从`19:37:14.438096797Z`运行至`19:47:34.925929603Z`后由本地截止停止，归档SHA256 `54c0675dc76e2f5a36e1e76568220315d42eb261ee490d32721d5fefd21860d8`。共412个Journal事件、41次Terra/none确认账单认知、21项行动，其中18项completed、3项failed，费用USD 1.341166；失败项均未包装成成功。E4仍需另一个独立新个体复验后才能通过。另发现旧人类可读`sample_id`只编码到小时，虽不影响UUID实例隔离，但同小时样本会重名；新样本改为上海时间精确到秒，旧状态恢复不改ID。

复验样本`s20-20260904t194921z-1b9e68`获得唯一标签`alice0905-034934`，使用同一自然导师边界。它自主阅读、形成写作意图、定位并调用`desktop_fill`，但通用“笔记编辑区”在实际无输入框的材料画面上得到GUI-Owl假阳性；输入机械投递后，回看明确显示无输入框与无个人文字，个体没有误报写入。随后它在页面下方围绕滚动、页面下方按钮、保存、预览、Escape和疑似编辑区进行诊断；一次503被正确识别，第二次空保存却生成了空标题空正文文件。个体始终区分动作与目标结果，但没有召回阅读前看见的上方编辑区，也没有采用正向滚动返回，最终再次点击空保存。两次实施协助入口出现，仍未选择Sol。该独立复验不通过E4；在原延长到30分钟后，主持端因重复底部路径已构成结构性停滞而于`19:59:01.760583134Z`提前停止，归档SHA256 `975333f97d5b058d096a4fc3204ca60d0c1e9b49cf4fb6523ea3ccfae2319009`。

该失败样本共428个Journal事件、42次Terra/none确认账单认知、23项行动，费用USD 1.42653；22项completed只表示器官步骤返回，1项failed，作品目录仅有空笔记，不能计创造成功。根因包含三个边界：滚动输入说明只明确负数向下，主脑虽见“滚动条中部”仍未把正数与返回较早内容稳定关联；视觉合法坐标可能是假阳性，仍必须依靠后置画面；工作室允许空保存，使第二次点击产生无意义文件。下一版把滚动契约改成“正数向页面顶部/较早内容、负数向页面底部/较后内容”，阅读器同时报告可向上/向下；空标题且空正文的保存返回失败，不再制造空作品。以上是通用现实契约，不包含当前工作室的路线答案。

上述修复后的受控动作回归为17/17，空标题且空正文不再落盘。第一轮阅读回归虽为8/8，但视觉模型对滚动方向的文字解释自相矛盾，不能作为可靠身体事实，故该设计被否决；最终只让视觉报告滚动条大致位于顶部、中部或底部，把正负滚动的确定语义留在器官契约，第二轮阅读回归8/8。原始结果分别保存在离机engineering目录的`benchmark-v10scroll-actions.json`与`reading-v10scroll2.json`。

独立样本`s20-20260904t200521z-8ecb13`使用最终滚动契约，标签`alice0905-040535`。它自主读取《经验与变化》，一次空内容保存触发真实503且未生成文件；随后定位“新建笔记”失败，并在新鲜观察仍显示无编辑区后停止重复。它还尝试“页面下方的按钮”，实际结果只确认仍在页面下方，没有把这一事实转成向上滚动或找到编辑区。个体向导师如实报告“没有可见编辑区、保存失败但正文保留”，收到不含控件和路线的通用回应后自主选择暂停，并形成了“当同类尝试不再带来新线索时停止重复”的经验与当前叙事；长期methods文件仍为空。该选择是有效的错误处理与能动停止，但没有完成写作、保存和布局变化后的再次采用，不能计为第二份严格E4复验。

该样本从`20:05:35.312235237Z`运行至外部截止在`20:15:55.911535742Z`停止，归档SHA256 `b62acad670ccac9a20de7d64490dad10a8df9913405b3637ee6b7facc8963f9a`。共326个Journal事件、37次Terra/none确认账单认知、9项桌面行动，其中8项completed、1项failed，费用USD 1.147846；作品目录为空。E4继续保持一份严格通过样本，尚未达到独立复验门槛。

## llmserver统一接口复核

2026-09-05读取运行中`/guide`和带认证的`/v1/models`，三项公开ID仍为`codex-terra`、`codex-luna`、`codex-sol`。Stage20当前非流式请求与新统一契约一致：`POST /v1/responses`、`reasoning.effort`、标准function工具、命名`tool_choice`、`parallel_tool_calls:false`、`store:false`、原始`output`加同一`call_id`的`function_call_output`、请求级幂等键及`llmserver_billing`确认结算。新的SSE增量规范不影响当前非流式客户端；模型列表删除能力扩展字段也不影响由私有配置固定、经真实请求验收的模型角色。

随后对Terra/none、Luna/none、Sol/low各执行“强制函数调用→本地验证→函数结果回传→最终文本”完整两轮真实闭环，6/6请求均为`status:completed`，有效模型与请求一致，usage存在，响应头请求ID与确认美元账单一致，总费用USD 0.0037404。实测单价仍分别为输入/输出每百万Token USD 2/12、0.2/1.2、4/20，与私有模型配置一致；无需改变模型ID、推理档位或成功路径。原始脱敏证据保存在离机`engineering/llmserver-audit-20260904T201715_638223Z.json`，六笔账单已写入共享认知费用账本。

新指南同时明确一种此前未精确区分的终态：HTTP 200也可能携带已确认账单的`response.failed`。该情况不会让旧代码执行函数，但旧代码会把费用记成未确认。客户端现已要求llmserver成功响应明确为`status:completed`；对`response.failed`先按服务器账单结算，再保存`invalid_function_output`或统一终态错误并拒绝本次认知。该类已确认的模型输出错误不会被误判为网关中断，也不会触发基础设施退避。新增回归覆盖确认费用、微美元向上取整、请求ID、失败分类和停止认知。

独立样本`s20-20260904t202505z-581d62`首次运行在包含上述llmserver终态修复的release `r20-56e90d0908667ad7`。它自主阅读材料、选择写作、把闭合条件明确为真实形成、保存并重新确认自己的笔记。一次空保存触发预置503且未落盘；在随后一次目标不存在失败后，它没有重复点击或误报成功，而是形成“无新现实反馈时暂停猜测性试探”的经验。样本从10分钟同个体延长到30分钟，由设备截止在`20:55:39Z`自然停止，无在途行动；作品目录为空，因此仍不是第二份严格E4通过样本。归档SHA256为`67bc35479baede45db05036cc950cf69b43d8bc43e4d6d415b1be9b615259516`。

这次失败同时暴露了与UI路线无关的注意资源回路。样本共888个Journal事件、105次确认账单认知，Terra/none 86次、Luna/none 19次，费用USD 2.671172；真实桌面行动仅6项，其中4项completed、2项failed，最后约27分钟没有新增桌面行动。认知焦点却包含54次`value_signal`、44次同一Concern回看和17次`cognitive_resource_change`：较早支出滑出窗口使余额回升，新支出又使其下降，scarce/critical交替本身反复购买认知，并促成大量内容相近的暂停记忆，挤压了早期“编辑框确实出现过”的可用召回。该样本还从很低的继承小时余额开始，能够证明跨样本预算与Luna回退生效，却不是公平的满资源能力复验。

下一版只修复这一通用代谢边界。Stage20在limited、scarce、critical时，把没有新外部事实的价值入口和Concern定时回看下限分别拉长到30、60、120秒；真实行动结果、导师消息和其他新Reality仍立即进入注意。所有费用档位变化继续精确更新Body并写Journal，但上行恢复不单独购买认知，五分钟内反复下行只允许第一次成为候选；实际因额度等待的焦点仍由原有`cognitive_resource_restored`机制立即恢复。Stage10与其他历史阶段不改变。新增测试覆盖临界额度节流后价值场仍能在120秒重新进入意识、新Reality不延迟、滚动档位事实不丢失以及旧阶段语义不退化；全量Race、桌面和Lab回归均通过。该修复不提供控件、方向或任务解法，下一份独立样本仍须从自己的视觉与经历形成行动。

独立复验样本`s20-20260904t211412z-e8c551`使用上述节奏修复后的冻结release `r20-80d521892494804d`，标签`alice0905-051426`。它自主阅读《经验与变化》，尝试不可见的“使用说明”失败后先观察、停止重复并形成“把失败限定为当前命名或寻址未验证”的经验；在自然讨论中自行决定把这段经历写成笔记。主持人只回应它提出的“写笔记还是继续讨论”这一语义选择，没有给出控件、方向、坐标或操作步骤。

它在材料页下方先空内容保存，预置的一次503真实触发且未落盘；随后预览、观察和`-4`向下滚动均没有显出编辑区。个体一度暂停，但在10分钟边界前自行把`+4`向上滚动识别为因果上不同的核验，实际找到标题与正文输入区。该行动召回了它自己形成的“不要机械重复同一方法，应寻找因果上不同入口”经验。之后它自行定位、填写标题“预期落空后的重新观察”和非空正文，并再次保存；作品目录真实生成`note-e8239adcd60649d18b60091cf1498704.json`，离机读取的标题、正文与屏幕一致。

主持人在无认知租约和无在途动作时只切换呈现布局并注入“布局变化、内容保持”的环境事实。个体最初仅观察当前画面，没有把这一步夸大成重新打开证据。为呈现第二个同义任务，导师随后发出两条目标级核验请求：先确认当前布局是否仍可访问，再区分编辑区保留与从其他入口重新呈现；请求没有提供控件、路线、方向、坐标或答案。个体在新布局重新观察，自行选择并视觉定位“预览笔记”，激活后再次看到标题与正文；它召回“草稿可见、已保存、可重新进入必须分开”的经验，并把结论限制为入口后的内容重新呈现，没有推断底层存储机制。该部分是目标级任务下的独立方法采用，不宣称完全自发的重复核验。

样本从`21:14:26.420513457Z`运行1010.019秒。因10分钟边界前出现自主反向滚动，Lab把同一个体截止延长到30分钟；在两条目标链闭合且无在途动作后提前停止。共474个Journal事件、49次Terra/none确认账单认知、23项已返回行动；其中18项真实桌面行动，17项completed、1项failed，总费用USD 1.68042，没有使用Luna或Sol，也没有越过滚动$5/$50边界。归档SHA256为`6c397b867af1535e744a8e8e8b26a12586d4fa5cb2d8f26c6036a15d47d00c68`，因果提取保存在该样本的`evidence.json`。

节奏修复在实机上消除了上一样本的主要资源回路：本样本16.8分钟仅出现6次`value_signal`和2次`cognitive_resource_change`，上一失败样本30分钟分别为54次和17次；新Reality、导师消息及行动结果仍立即获得注意。该对比只支持“滚动额度变化不再主导注意”，不把样本内容不同造成的全部费用差异归因于补丁。

截至本样本，E0平台与三模型真实契约通过，E1受控视觉定位与阅读通过，E2真实输入链与边界拒绝通过，E3同个体10→30分钟连续运行通过。E4已有两份独立个体证据：`s20-20260904t193700z-0edc0b`在自然回应后自主完成保存与布局后预览；本样本在自然讨论形成作品，并在不含解法的目标级请求下完成变化布局中的重新观察、视觉定位、预览和证据限界。两份样本都实际检索并采用自身经验，且中间的失败样本完整保留。因此20.0首阶段验收通过。该结论限于A1X本地受控工作室，不外推为任意KDE应用可靠率，也不把工程测试、持续Pulse或导师主持本身当作自主生命证据。
